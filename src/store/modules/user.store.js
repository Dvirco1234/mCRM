import { userService } from '../../services/user.service'

export const userStore = {
    state: {
        loggedInUser: null,
    },
    getters: {

    },
    mutations: {
        // saveUserPrefs(pref) {
        //     userService.saveUserPrefs(pref)
        // }
    },
    actions: {

    }
}